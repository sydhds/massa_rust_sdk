[workspace]
members = [
    "massa_rust_sc",
    "massa_rust_web3",
    "hello_world",
    "hello_world_scripts",
]
resolver = "2"

[profile.release]
strip = true
# # strip = "debuginfo" # strip but keep function name
lto = true # longer link time but can reduce size

